# 6. Internationalization (i18n)

**Related TRDs**: [07-web-frontend-architecture](./07-web-frontend-architecture.md), [13-notifications](./13-notifications.md), [14-newsletter](./14-newsletter.md)  
**Related ADRs**: [ADR-002](./adr/002-trilingual-day1.md), [ADR-015](./adr/015-frontend-library-stack.md)  
**Phase**: MVP (Phase 1)

---

### Supported Languages

- **English** (default) - en-US
- **Korean** - ko-KR
- **Spanish** - es-ES

### Language Selection & Storage

- Parent selects preferred language during registration.
- Language preference stored in User record (`language` field).
- Language sent in JWT token for every request.
- Client includes `Accept-Language` header on API requests (e.g., `Accept-Language: ko-KR`).
- Server uses JWT language preference as primary, falls back to `Accept-Language` header.

### Server-Side Rendering

All server-generated content (notifications, emails, newsletters) is rendered in the recipient's preferred language:

1. Fetch notification template for the recipient's language.
2. Render template with personalization tokens.
3. Dispatch to appropriate channel (SMS, email, push, etc.).

### Client-Side i18n

#### Framework

- Uses **react-i18next** with **i18next** core engine.
- Translation files stored in `packages/shared/src/i18n/`.
- Lazy-loaded per namespace — each app loads only what it needs.

#### Namespace Organization

| Namespace | Contents | Used By |
|-----------|----------|---------|
| `common` | Shared UI labels ("Save", "Cancel", "Delete", "Loading..."), navigation, generic errors | All apps |
| `auth` | Login, OTP, 2FA, logout labels | Parent, Admin |
| `registration` | Registration form labels, COPPA consent, member profile fields | Parent, Admin |
| `attendance` | Check-in status, attendance history labels, absence alerts | Parent, Admin |
| `payments` | Invoice labels, payment status, billing plan names, refund language | Parent, Admin |
| `simsa` | Belt test labels, eligibility, results, certificate text | Parent, Admin |
| `notifications` | Notification titles, preference labels | Parent, Admin |
| `newsletter` | Newsletter labels, editor UI | Parent (read), Admin (edit) |
| `feed` | Activity feed labels, reaction names | Parent (read), Admin (post) |
| `reports` | Report names, chart labels, filter labels | Admin |
| `settings` | Settings section labels, configuration descriptions | Parent, Admin |
| `monitor` | Display labels, schedule labels | Monitor |
| `validation` | Validation error messages ("Required field", "Invalid email", "Too short") | All apps |

#### File Structure

```
packages/shared/src/i18n/
├── en/
│   ├── common.json
│   ├── auth.json
│   ├── registration.json
│   ├── attendance.json
│   ├── payments.json
│   ├── simsa.json
│   ├── notifications.json
│   ├── newsletter.json
│   ├── feed.json
│   ├── reports.json
│   ├── settings.json
│   ├── monitor.json
│   └── validation.json
├── ko/
│   └── ... (same structure)
└── es/
    └── ... (same structure)
```

#### Language Detection & Switching

1. On app load: check Zustand auth store for `user.language`.
2. If no user (not logged in): check `navigator.language` → map to `en`, `ko`, or `es`.
3. Fallback: `en` (English).
4. Language switchable at runtime in Settings. Calls `i18n.changeLanguage(lang)` and updates Zustand preferences store.
5. After login, JWT `language` claim takes priority (syncs cross-device).

#### Date/Time/Currency Formatting

Uses **date-fns** locale modules:

| Format | English (en-US) | Korean (ko-KR) | Spanish (es-ES) |
|--------|-----------------|-----------------|-----------------|
| Date (short) | `02/25/2026` | `2026년 2월 25일` | `25/02/2026` |
| Date (long) | `February 25, 2026` | `2026년 2월 25일 화요일` | `25 de febrero de 2026` |
| Time | `3:30 PM` | `15:30` | `15:30` |
| Currency | `$99.99` | `$99.99` | `$99.99` |
| Relative time | `3 days ago` | `3일 전` | `hace 3 días` |

Currency always USD (ADR-001). Uses `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' })`.

**Direction:** All languages LTR. No RTL support needed.

#### Kiosk App i18n

- Kiosk displays greetings in member's preferred language (from member profile).
- UI labels on kiosk are in gym's default language.
- Minimal text on kiosk — mostly visual/audio feedback.

### Notification Templates

Admin creates notification templates with per-language variants:

**Template Storage**:
- One `NotificationTemplate` record per language per notification type.
- Template body contains personalization tokens: `{{member_name}}`, `{{days_absent}}`, `{{balance_due}}`, `{{belt_level}}`, `{{simsa_date}}`, `{{gym_name}}`, etc.

**Example**:
- English: "Hi {{parent_name}}, {{member_name}} checked in at {{gym_name}} at {{check_in_time}}."
- Korean: "안녕하세요 {{parent_name}}님, {{member_name}}이(가) {{check_in_time}}에 {{gym_name}}에 입장했습니다."
- Spanish: "Hola {{parent_name}}, {{member_name}} se registró en {{gym_name}} a las {{check_in_time}}."

### Cultural UX Patterns

- **Korean Communications**: Use appropriate honorific forms (존칭어). Address parents as "님" (nim). Use formal verb endings.
- **Spanish Communications**: Use formal "usted" form instead of informal "tú".
- **Date/Time Formatting**:
  - English: MM/DD/YYYY, 12-hour time (e.g., "02/25/2026, 3:30 PM")
  - Korean: YYYY년 MM월 DD일, 24-hour time (e.g., "2026년 2월 25일, 15:30")
  - Spanish: DD/MM/YYYY, 24-hour time (e.g., "25/02/2026, 15:30")
- **Currency**: Always USD, but formatted per locale:
  - English: "$99.99"
  - Korean: "$99.99" (same)
  - Spanish: "$99,99" (comma as decimal separator in some regions)


### Implementation Notes

> _This section will be updated as the feature is implemented._

- **Module location**: _TBD_
- **Key files**: _TBD_
- **Actual endpoints**: _TBD_
- **Deviations from spec**: _None yet_
- **Edge cases discovered**: _None yet_
- **Configuration**: _TBD_
